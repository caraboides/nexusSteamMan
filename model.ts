export type ProtonConfig = { appId: string, mode: string, tool?: string }

export type Game = {
    appId: String
    name: String
    installdir: String
    accountId: String
    protonConfig: ProtonConfig
}

